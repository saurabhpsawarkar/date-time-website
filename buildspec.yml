version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm init -y
      - npm install express
  build:
    commands:
      - echo Build step complete
artifacts:
  files:
    - '**/*'
